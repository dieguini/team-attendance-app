name: learn-github-actions

run-name: ${{ github.actor }} is learning GitHub Actions

on: [push]

jobs:
  first-version:
    runs-on: ubuntu-latest
    steps:
      - run: echo "First commit learn ✔"